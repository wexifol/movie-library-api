from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base
import os


DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./movies.db")


engine = create_engine(
DATABASE_URL,
connect_args={"check_same_thread": False}
)


SessionLocal = sessionmaker(bind=engine, autoflush=False)
Base = declarative_base()




def get_db():
db = SessionLocal()
try:
yield db
finally:
db.close()

«Прикольно оглянуться на первый месяц тренировок с сентября. Начинал буквально через силу — после каждой тренировки всё ныло, и мысль о следующем через день была немного пугающей. Но где-то после второй недели что-то щёлкнуло. Тело втянулось, дыхание перестало сбиваться так быстро, а привычный вес начал казаться легче. Самое главное — появилась не ажиотажная, а спокойная уверенность, что я это могу. Не гнал за объёмами, делал посильно, но регулярно. И эта регулярность, без надрыва, дала тот самый эффект: от "ах, как тяжело" до "ого, а ведь уже легко". Месяц прошёл не зря.»

